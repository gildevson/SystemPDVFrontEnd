import { ListaDeUsuariosComponent } from './ListaDeUsuarios.component.component';

describe('ListaDeUsuariosComponent', () => {
  it('should create component', () => {
    const component = new ListaDeUsuariosComponent(null as any);
    expect(component).toBeTruthy();
  });
});
